# Contains the Angular code for the officer page -> will generate all the pictures and modals based on info through JSON file
angular.module('officers',[]).controller 'officerCtrl', [
	'$scope'
	($scope) ->
		$scope.test = 'Hello World!'
		return
]