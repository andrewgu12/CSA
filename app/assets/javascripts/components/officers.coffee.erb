OfficerModals = React.createClass
	# displayName: 'officerModals'
	render: ->
		React.DOM.div
			className: 'photo'
			React.DOM.h2
				className: 'officer'
React.render React.createElement(OfficerModals, {}), $('#officerModals')
# $(document).ready reactReady
# $(document).on 'page:change' ->
# 	React.render React.createElement(officerModals, {}), $('#officerPics')