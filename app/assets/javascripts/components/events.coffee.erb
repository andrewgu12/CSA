# an react try for events page -> let's try angular one more f-ing time
# reactReady = ->
#     eventID = (window.location.href).split('#')[1]  
#     console.log eventID  
#     EventPage = React.createClass(
#         loadInfoFromFile: ->
#             $.ajax
#               url: @props.url
#               dataType: 'json'
#               cache: false
#               success: ((data) ->
#                 @setState data: data.events
#                 console.log @state.data                
#                 return
#               ).bind(this)
#               error: ((xhr, status, err) ->
#                 console.error @props.url, status, err.toString()
#                 return
#               ).bind(this)
#             return
#         getInitialState: ->
#             {data: [] }
#         componentDidUpdate: ->
#             console.log 'yes'
#         componentDidMount: ->            
#             @loadInfoFromFile()            
#             return
#         render: ->
#             React.createElement 'div', {className: 'yo!'}            
#     )
#     React.render React.createElement(EventPage, {url: "events.json"}), $('.event').get 0
# $(document).ready reactReady
# $(document).on 'page:load', reactReady
# $(document).on 'page:change', reactReady